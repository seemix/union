// @ts-check

/**
 * @type {import('next').NextConfig}
 */
const config = {
    // output: 'export',
    distDir: 'dist',
    images: {
        domains: ['baptist.org.md'],
    },
};

export default config;